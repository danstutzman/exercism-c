#include "stdio.h"
#include "word_count.h"

int word_count(const char *input_text, word_count_word_t * words) {
  (void)input_text; // unused
  (void)words; // unused
  return 0;
}

int main() {
  printf("test\n");
}
